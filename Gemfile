source "https://rubygems.org"

# This mirrors what GitHub Pages actually builds with, so what you see
# locally matches what goes live.
gem "github-pages", group: :jekyll_plugins

# Needed for live-reloading local preview (`jekyll serve --livereload`)
gem "webrick"

